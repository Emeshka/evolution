export enum ActionSign {
  M = '*',
  A = '+',
  S = '-',
  D = '/',
}

export enum ComparativeSign {
  // equality is a very rare case and can be achieved as a narrow margin between G and L
  G = '>',
  L = '<',
}
